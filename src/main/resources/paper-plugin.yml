name: BetterDeathMessagesPlugin
description: "A very simple plugin which displays death messages for villagers."
version: '1.0'
authors: ["rolyPolyVole"]
contributors: ["Slqmy"]
main: org.rolypolyvole.better_death_messages.BetterDeathMessagesPlugin
api-version: '1.20'
load: POSTWORLD
dependencies:
  {
    server:
      {
        DiscordSRV: {
          load: BEFORE,
          required: false
        }
      }
  }